package ocr

import (
	"testing"
)

func TestRecognizeFolder(t *testing.T) {
	/*
		fileName := "/home/ocr/result1"
		results := RecognizeFolder(fileName, "chi_sim")
		for _, result := range results {
			fmt.Println(result.Text)
		}
	*/
}
